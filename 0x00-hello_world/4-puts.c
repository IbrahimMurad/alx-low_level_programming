#include <stdio.h>

/**
 * main - Entry point
 *
 * This program is to use puts function
 * puts prints "Programming is like building a multilingual puzzle"
 *
 * Return: Always (0) (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
